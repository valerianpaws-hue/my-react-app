# NPM Configuration for Netlify Build
# This prevents optional dependencies from failing the build

# Skip optional dependencies that might require native compilation
optional=false

# Use legacy peer deps to avoid conflicts
legacy-peer-deps=true

# Disable scripts that might trigger native builds
ignore-scripts=false

# Set registry
registry=https://registry.npmjs.org/

# Increase timeout for slow networks
fetch-timeout=300000
